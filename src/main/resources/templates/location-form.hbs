{{#partial "content"}}

    {{#if editLocation}}
        <h1>Select the location you'd like to edit..</h1>
        <form action="/locations/update" method="post">
            <select name="editLocationId" id="editLocationId">
                {{#each locations}}
                    <option value="{{id}}">{{name}}</option>
                {{/each}}
            </select>
            <label for="newLocationName">Edit this location's name here:</label>
            <input id="newLocationName" name="newLocationName" type="text" placeholder="new location name">

    {{else}}
            <h1>Add a new Location!</h1>

        <form action="/locations" method="post">
            <label for="name">Location Name</label>
            <input id="name" name="name" type="text">

    {{/if}}
        <button type="submit" class="btn btn-default">Go!</button>
    </form>
{{/partial}}

{{> layout.hbs}}